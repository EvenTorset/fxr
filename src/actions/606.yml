type: 606
name: LegacyTracer
slot: Appearance
meta:
  isAppearance: true
  isParticle: true
desc:
  en-US: |
    Defines a tracer particle, which leaves a textured trail where it moves.

    This is an older version of {@link Tracer} with fewer features.
  
    The name is from Elden Ring's RTTI, where it is named "GXFXCGAppearanceTracer".
  zh-CN: |
    定义一个追踪粒子，它会在移动时留下纹理轨迹。

    这是 {@link Tracer} 的旧版本，功能较少。
  
    该名称来自《艾尔登法环》的 RTTI，其中它被命名为“GXFXCGAppearanceTracer”。
properties:
  texture:
    type: ScalarValue
    field: int
    default: 1
    argument: Constant0
    resource: texture
    textureType: a
    desc:
      en-US: |
        Texture ID.
      zh-CN: |
        纹理 ID。
  blendMode:
    type: BlendMode | ScalarProperty
    field: int
    enum: BlendMode
    default: BlendMode.Normal
    argument: Constant0
    desc:
      en-US: |
        Blend mode.
      zh-CN: |
        混合模式。
  width:
    type: ScalarValue
    default: 1
    argument: ParticleAge
    scale: instanceSize
    desc:
      en-US: |
        The width of the trail.
      zh-CN: |
        拖尾的宽度。
  widthMultiplier:
    type: ScalarValue
    default: 1
    argument: EmissionTime
    desc:
      en-US: |
        Multiplier for {@link width}.
      zh-CN: |
        {@link width} 的乘数。
  startFadeEndpoint:
    type: ScalarValue
    argument: ParticleAge
    desc:
      en-US: |
        Percentage along the trail from the start of the trail where the trail's opacity will start fading out towards the start. The trail will have 0 opacity at the start, and it will linearly increase towards full opacity at the point represented by this percentage.

        The percentage is of the way from the start of the trail to the end of it, so if the trail gets longer or shorter, this point will move with it.

        Values greater than 100 will make the trail never reach full opacity. For example, a value of 200 will make the trail fade from 0 opacity at the start to 50% (100% / 200%) at the end of the trail.

        If this value and {@link endFadeEndpoint} sum to a value greater than 100, the trail will be "split" at the point controlled by this value and each part will fade separately based on the repsective value. For example, if this is set to 50 and {@link endFadeEndpoint} is 1000, the first half of the trail will fade normally from 0 opacity at the start to full opacity at the mid point, but the other half will fade from 5% opacity ((100% - 50%) / 1000%) at the mid point to 0 at the end. Where the two parts meet, the opacity will fade from one to the other across a single segment of the trail.
      zh-CN: |
        沿拖尾从起点开始的百分比，拖尾的不透明度将在此处开始向起点衰减。拖尾在起点的透明度为 0，并将在该百分比代表的点线性增加到完全不透明。

        该百分比是从拖尾起点到终点的路程，因此如果拖尾变长或变短，该点也会随之移动。

        大于 100 的值将使拖尾永远无法达到完全不透明。例如，值为 200 将使拖尾从起点的 0 不透明度淡化到终点的 50% (100% / 200%)。

        如果此值与 {@link endFadeEndpoint} 之和大于 100，拖尾将在此值控制的点处“分裂”，每部分将根据各自的值单独衰减。例如，如果此值设置为 50 而 {@link endFadeEndpoint} 为 1000，则拖尾的前半部分将正常地从起点的 0 不透明度淡化到中点的完全不透明，但另一半将从中点的 5% 不透明度 ((100% - 50%) / 1000%) 淡化到终点的 0。在两部分相遇的地方，不透明度将在拖尾的单个段上从一个过渡到另一个。
  endFadeEndpoint:
    type: ScalarValue
    argument: ParticleAge
    desc:
      en-US: |
        Percentage along the trail from the end of the trail where the trail's opacity will start fading out towards the end. The trail will have full opacity at the point represented by this percentage, and it will linearly decrease towards 0 opacity at the end.

        The percentage is of the way from the end of the trail to the start of it, so if the trail gets longer or shorter, this point will move with it.

        Values greater than 100 will make the trail never reach full opacity. For example, a value of 200 will make the trail fade from 0 opacity at the end to 50% (100% / 200%) at the start of the trail.

        If this value and {@link startFadeEndpoint} sum to a value greater than 100, the trail will be "split" at the point controlled by {@link startFadeEndpoint} and each part will fade separately based on the repsective value. For example, if {@link startFadeEndpoint} is set to 50 and this is 1000, the first half of the trail will fade normally from 0 opacity at the start to full opacity at the mid point, but the other half will fade from 5% opacity ((100% - 50%) / 1000%) at the mid point to 0 at the end. Where the two parts meet, the opacity will fade from one to the other across a single segment of the trail.
      zh-CN: |
        沿拖尾从终点开始的百分比，拖尾的不透明度将在此处开始向终点衰减。拖尾在该百分比代表的点处将完全不透明，并在终点处线性减少到 0 不透明度。

        该百分比是从拖尾终点到起点的路程，因此如果拖尾变长或变短，该点也会随之移动。

        大于 100 的值将使拖尾永远无法达到完全不透明。例如，值为 200 将使拖尾从终点的 0 不透明度淡化到起点的 50% (100% / 200%)。

        如果此值与 {@link startFadeEndpoint} 之和大于 100，拖尾将在 {@link startFadeEndpoint} 控制的点处“分裂”，每部分将根据各自的值单独衰减。例如，如果 {@link startFadeEndpoint} 设置为 50 而此值为 1000，则拖尾的前半部分将正常地从起点的 0 不透明度淡化到中点的完全不透明，但另一半将从中点的 5% 不透明度 ((100% - 50%) / 1000%) 淡化到终点的 0。在两部分相遇的地方，不透明度将在拖尾的单个段上从一个过渡到另一个。
  color1:
    type: Vector4Value
    components: 4
    default: [1, 1, 1, 1]
    argument: ParticleAge
    color: true
    desc:
      en-US: |
        Color multiplier.

        Values in this will be clamped to the 0-1 range. To use values outside of this range, see {@link color3}.
      zh-CN: |
        颜色乘数。

        此处的数值将被限制在 0-1 范围内。要使用超出此范围的值，请参阅 {@link color3}。
    see:
      - color2
      - color3
  color2:
    type: Vector4Value
    components: 4
    default: [1, 1, 1, 1]
    argument: EmissionTime
    color: true
    desc:
      en-US: |
        Color multiplier.

        Values in this will be clamped to the 0-1 range. There is no equivalent property with unrestricted values based on the emission time of the particle, but {@link color3} is still multiplicative with this and can be used to scale the values indirectly.
      zh-CN: |
        颜色乘数。

        此处的数值将被限制在 0-1 范围内。虽然没有基于粒子发射时间的不受限制值的等效属性，但 {@link color3} 仍可与其相乘，并可用于间接缩放数值。
    see:
      - color1
      - color3
  color3:
    type: Vector4Value
    components: 4
    default: [1, 1, 1, 1]
    argument: ParticleAge
    color: primary
    desc:
      en-US: |
        Color multiplier.

        Values in this are unrestricted and can go above 1. If you want values to be clamped to the 0-1 range, see {@link color1}.
      zh-CN: |
        颜色乘数。

        此处的数值不设限，可以超过 1。如果希望将数值限制在 0-1 范围内，请参阅 {@link color1}。
    see:
      - color1
      - color2
  alphaFadeThreshold:
    type: ScalarValue
    argument: ParticleAge
    desc:
      en-US: |
        Parts of the particle with less opacity than this threshold will be invisible. The range is 0-255.

        Values above this threshold will be stretched to fill the alpha range, so values near the threshold will be less visible, creating a smooth transition between the parts that have been hidden by the threshold and the ones that are still visible.
      zh-CN: |
        不透明度低于此阈值的粒子部分将不可见。范围是 0-255。

        高于此阈值的值将被拉伸以填充 Alpha 范围，因此接近阈值的值将不太明显，从而在被阈值隐藏的部分和仍然可见的部分之间创建平滑过渡。
  frameIndex:
    type: ScalarValue
    argument: ParticleAge
    desc:
      en-US: |
        The index of the frame to show from the texture atlas. Can be animated using a {@link PropertyFunction.Linear linear property} or similar.

        Seemingly identical to {@link frameIndexOffset}? The sum of these two properties is the actual frame index that gets used.
      zh-CN: |
        要从纹理图集中显示的帧索引。可以使用 {@link PropertyFunction.Linear 线性属性}或类似属性进行动画处理。

        似乎与 {@link frameIndexOffset} 相同？这两个属性的总和是实际使用的帧索引。
  frameIndexOffset:
    type: ScalarValue
    argument: ParticleAge
    desc:
      en-US: |
        Seemingly identical to {@link frameIndex}? The sum of these two properties is the actual frame index that gets used.
      zh-CN: |
        似乎与 {@link frameIndex} 相同？这两个属性的总和是实际使用的帧索引。
  textureFraction:
    type: ScalarValue
    default: 0.1
    argument: ParticleAge
    desc:
      en-US: |
        Controls how much of the texture's width is used per segment. If {@link attachedUV} is enabled, this instead controls how much of the texture's width to use for the entire trail.
      zh-CN: |
        控制每一段使用的纹理宽度比例。如果启用了 {@link attachedUV}，则改为控制整个拖尾使用的纹理宽度比例。
  speedU:
    type: ScalarValue
    argument: ParticleAge
    time: true
    desc:
      en-US: |
        Controls how fast the UV coordinates should move horizontally.
      zh-CN: |
        控制 UV 坐标水平移动的速度。
  varianceV:
    type: ScalarValue
    argument: ParticleAge
    desc:
      en-US: |
        Controls how much the UV coordinates should be randomly offset by per segment.
      zh-CN: |
        控制每段 UV 坐标随机偏移的量。
  unk_ds3_p1_13:
    type: ScalarValue
    default: -1
  rgbMultiplier:
    type: ScalarValue
    default: 1
    argument: ActiveTime
    desc:
      en-US: |
        Scalar multiplier for the color that does not affect the alpha. Effectively a brightness multiplier.
      zh-CN: |
        不影响 Alpha 的颜色标量乘数。实际上是亮度乘数。
  alphaMultiplier:
    type: ScalarValue
    default: 1
    argument: ActiveTime
    desc:
      en-US: |
        Alpha multiplier.
      zh-CN: |
        Alpha 乘数。
  distortionIntensity:
    type: ScalarValue
    argument: ActiveTime
    desc:
      en-US: |
        Controls the intensity of the distortion effect. At 0, there is no distortion at all.
      zh-CN: |
        控制扭曲效果的强度。为 0 时，完全没有扭曲。
    see:
      - normalMap
  unk_ds3_p2_3:
    type: Vector4Value
    components: 4
    default: [1, 1, 1, 1]
  unk_ds3_p2_4:
    type: Vector4Value
    components: 4
    default: [1, 1, 1, 1]
  unk_ds3_p2_5:
    type: Vector4Value
    components: 4
    default: [1, 1, 1, 1]
  alphaThreshold:
    type: ScalarValue
    argument: ActiveTime
    desc:
      en-US: |
        Parts of the particle with less opacity than this threshold will be invisible. The range is 0-255.

        This threshold creates a hard cut-off between visible and not visible, which is unlike the {@link alphaFadeThreshold}.
      zh-CN: |
        不透明度低于此阈值的粒子部分将不可见。范围是 0-255。

        此阈值在可见和不可见之间创建一个硬性截止，这与 {@link alphaFadeThreshold} 不同。
  orientation:
    type: TracerOrientationMode
    field: int
    enum: TracerOrientationMode
    default: TracerOrientationMode.LocalZ
    desc:
      en-US: |
        Tracer orientation mode. See {@link TracerOrientationMode} for more information.
      zh-CN: |
        拖尾朝向模式。有关更多信息，请参阅 {@link TracerOrientationMode}。
  normalMap:
    field: int
    default: 0
    resource: texture
    textureType: 'n'
    desc:
      en-US: |
        Normal map texture ID.

        This is used to control the distortion effect of the trail.
      zh-CN: |
        法线贴图纹理 ID。

        这用于控制拖尾的扭曲效果。
    see:
      - distortionIntensity
  segmentInterval:
    field: float
    default: 0
    time: inv
    desc:
      en-US: |
        The trail is made up of multiple quads, or *segments*. This controls how many seconds to wait between new segments being created. Lower values produce a smoother trail.
      zh-CN: |
        拖尾由多个四边形或*段*组成。此项控制创建新段之间等待的秒数。较低的值会产生更平滑的拖尾。
  segmentDuration:
    field: float
    default: 1
    time: tracerDuration
    scale: instanceSize
    desc:
      en-US: |
        The trail is made up of multiple quads, or *segments*. This controls how long each segment should last in seconds.
      zh-CN: |
        拖尾由多个四边形或*段*组成。此项控制每个段应持续的秒数。
  concurrentSegments:
    field: int
    default: 100
    desc:
      en-US: |
        The trail is made up of multiple quads, or *segments*. This controls how many segments may exist at the same time.
      zh-CN: |
        拖尾由多个四边形或*段*组成。此项控制同一时间可能存在的段数。
  segmentSubdivision:
    field: int
    desc:
      en-US: |
        The trail is made up of multiple quads, or *segments*. This controls how many times each completed segment should be subdivided. Higher values makes the trail look smoother.

        A "completed" segment is any segment that is not the leading one. The leading segment has one side attached to the end of the previous segment and the other attached to the tracer source, and is always a simple quad.
      zh-CN: |
        拖尾由多个四边形或*段*组成。此项控制每个完成的段应细分的次数。较高的值使拖尾看起来更平滑。

        “完成”的段是指除引导段以外的任何段。引导段的一侧连接到前一段的末端，另一侧连接到拖尾源，并且始终是一个简单的四边形。
  unk_ds3_f1_8:
    field: float
  fadeOutTime:
    field: float
    desc:
      en-US: |
        When the particle is meant to terminate, the trail will linger for this many seconds and its opacity will fade to 0 in that time.
      zh-CN: |
        当粒子即将终止时，拖尾将停留这么多秒，并且其不透明度将在这段时间内衰减为 0。
  columns:
    field: int
    default: 1
    desc:
      en-US: |
        To split the texture into multiple animation frames, this value must be set to the number of columns in the texture. It should equal `textureWidth / frameWidth`.
      zh-CN: |
        要将纹理分割成多个动画帧，必须将此值设置为纹理中的列数。它应等于 `textureWidth / frameWidth`。
    see:
      - totalFrames
  totalFrames:
    field: int
    default: 1
    desc:
      en-US: |
        To split the texture into multiple animation frames, this value must be set to the total number of frames in the texture.
      zh-CN: |
        要将纹理分割成多个动画帧，必须将此值设置为纹理中的总帧数。
    see:
      - columns
  attachedUV:
    field: bool
    default: true
    desc:
      en-US: |
        Controls whether or not the UV of the trail should be attached to the node or not. If it is attached, the texture will slide along the segments to follow the source wherever it moves, as if it was a flag attached to a pole. If it is not attached, the texture will stay where it was when the segment was created, like a skid mark on a road where the road is the segments and the mark is the texture, it wouldn't follow the car/node that made it.
      zh-CN: |
        控制拖尾的 UV 是否应附着到节点。如果附着，纹理将沿着段滑动以跟随源的移动，就像旗帜附着在旗杆上一样。如果不附着，纹理将停留在创建段时的位置，就像道路上的刹车痕迹，道路是段，痕迹是纹理，它不会跟随制造它的汽车/节点。
  unk_ds3_f1_13:
    field: int
    default: -1
  unk_ds3_f1_14:
    field: int
    default: -1
  unk_ds3_f1_15:
    field: int
  unk_er_f1_14:
    field: int
    default: 1
  unk_er_f1_15:
    field: int
    default: 1
  unk_er_f1_16:
    field: int
  unk_ds3_f2_0:
    field: int
  unk_ds3_f2_1:
    field: int
    desc:
      en-US: |
        Unknown integer. Possibly boolean?
      zh-CN: |
        未知整数。可能是布尔值？
  unk_ds3_f2_2:
    field: int
    default: 8
  unk_ds3_f2_3:
    field: int
  bloom:
    field: bool
    desc:
      en-US: |
        Controls whether or not the particles have an additional bloom effect controlled by {@link bloomColor}.

        When enabled, this also allows bloom from other particles to be seen through this particle.

        Note:
        - This has no effect if the "Effects Quality" setting is set to "Low".
        - This does not affect the natural bloom effect from high color values.
      zh-CN: |
        控制粒子是否具有由 {@link bloomColor} 控制的额外泛光效果。

        启用时，这也允许透过此粒子看到其他粒子的泛光。

        注意：
        - 如果“特效质量”设置设为“低”，则此项无效。
        - 这不影响高颜色值产生的自然泛光效果。
    see:
      - bloomColor
  bloomColor:
    field: vec4
    components: 4
    default: [1, 1, 1, 0]
    color: true
    desc:
      en-US: |
        Controls the color of the additional bloom effect. The colors of the particle will be multiplied with this color to get the final color of the bloom effect.

        Note:
        - This has no effect if the "Effects Quality" setting is set to "Low".
        - This does not affect the natural bloom effect from high color values.
      zh-CN: |
        控制额外泛光效果的颜色。粒子的颜色将与此颜色相乘，以获得泛光效果的最终颜色。

        注意：
        - 如果“特效质量”设置设为“低”，则此项无效。
        - 这不影响高颜色值产生的自然泛光效果。
    see:
      - bloom
  unk_ds3_f2_9:
    field: float
  unk_ds3_f2_10:
    field: int
  unk_ds3_f2_11:
    field: int
  unk_ds3_f2_12:
    field: int
  unk_ds3_f2_13:
    field: int
  minFadeDistance:
    field: float
    default: -1
    scale: distanceIfNotMinusOne
    desc:
      en-US: |
        This controls a point where the opacity of a particle will start to fade to 0 near the {@link minDistance minimum view distance}. At {@link minDistance}, the opacity will be 0, and it will linearly approach 1 as the distance between the camera and the particle approaches this distance.

        This requires {@link minDistance} to be set to a positive value or 0. This distance limit can be disabled by setting this and minDistance to -1.
      zh-CN: |
        控制粒子不透明度开始在 {@link minDistance 最小视图距离} 附近衰减为 0 的点。在 {@link minDistance} 处，不透明度为 0，随着摄像机与粒子之间的距离接近此距离，不透明度将线性接近 1。

        这要求 {@link minDistance} 设置为正值或 0。可以通过将此值和 minDistance 设置为 -1 来禁用此距离限制。
    see:
      - minDistance
      - maxFadeDistance
      - maxDistance
      - minDistanceThreshold
      - maxDistanceThreshold
  minDistance:
    field: float
    default: -1
    scale: distanceIfNotMinusOne
    desc:
      en-US: |
        Minimum view distance. If a particle is closer than this distance from the camera, it will be hidden. Can be set to -1 to disable the limit.

        This requires {@link minFadeDistance} to be set to a positive value or 0.

        This is different from {@link minDistanceThreshold}, as this controls the start of a distance range that has smooth transitions at each end, while the threshold value is a hard cut-off.
      zh-CN: |
        最小视图距离。如果粒子与摄像机的距离小于此距离，它将被隐藏。可以设置为 -1 以禁用限制。

        这要求 {@link minFadeDistance} 设置为正值或 0。

        这与 {@link minDistanceThreshold} 不同，因为此项控制的是两端具有平滑过渡的距离范围的起点，而阈值是硬性截止。
    see:
      - minFadeDistance
      - maxFadeDistance
      - maxDistance
      - minDistanceThreshold
      - maxDistanceThreshold
  maxFadeDistance:
    field: float
    default: -1
    scale: distanceIfNotMinusOne
    desc:
      en-US: |
        This controls a point where the opacity of a particle will start to fade to 0 near the {@link maxDistance maximum view distance}. At {@link maxDistance}, the opacity will be 0, and it will linearly approach 1 as the distance between the camera and the particle approaches this distance.

        This requires {@link maxDistance} to be set to a positive value or 0. This distance limit can be disabled by setting this and minDistance to -1.
      zh-CN: |
        控制粒子不透明度开始在 {@link maxDistance 最大视图距离} 附近衰减为 0 的点。在 {@link maxDistance} 处，不透明度为 0，随着摄像机与粒子之间的距离接近此距离，不透明度将线性接近 1。

        这要求 {@link maxDistance} 设置为正值或 0。可以通过将此值和 minDistance 设置为 -1 来禁用此距离限制。
    see:
      - minFadeDistance
      - minDistance
      - maxDistance
      - minDistanceThreshold
      - maxDistanceThreshold
  maxDistance:
    field: float
    default: -1
    scale: distanceIfNotMinusOne
    desc:
      en-US: |
        Minimum view distance. If a particle is farther away than this distance from the camera, it will be hidden. Can be set to -1 to disable the limit.

        This requires {@link maxFadeDistance} to be set to a positive value or 0.

        This is different from {@link maxDistanceThreshold}, as this controls the start of a distance range that has smooth transitions at each end, while the threshold value is a hard cut-off.
      zh-CN: |
        最小视图距离。如果粒子与摄像机的距离大于此距离，它将被隐藏。可以设置为 -1 以禁用限制。

        这要求 {@link maxFadeDistance} 设置为正值或 0。

        这与 {@link maxDistanceThreshold} 不同，因为此项控制的是两端具有平滑过渡的距离范围的起点，而阈值是硬性截止。
    see:
      - minFadeDistance
      - minDistance
      - maxFadeDistance
      - minDistanceThreshold
      - maxDistanceThreshold
  minDistanceThreshold:
    field: float
    default: -1
    scale: distanceIfNotMinusOne
    desc:
      en-US: |
        A hard cut-off point for the distance between the camera and the particle. If a particle is closer than this distance from the camera, it will be hidden. Can be set to -1 to disable the limit.
      zh-CN: |
        摄像机与粒子之间距离的硬性截止点。如果粒子与摄像机的距离小于此距离，它将被隐藏。可以设置为 -1 以禁用限制。
    see:
      - minFadeDistance
      - minDistance
      - maxFadeDistance
      - maxDistance
      - maxDistanceThreshold
  maxDistanceThreshold:
    field: float
    default: -1
    scale: distanceIfNotMinusOne
    desc:
      en-US: |
        A hard cut-off point for the distance between the camera and the particle. If a particle is farther away than this distance from the camera, it will be hidden. Can be set to -1 to disable the limit.
      zh-CN: |
        摄像机与粒子之间距离的硬性截止点。如果粒子与摄像机的距离大于此距离，它将被隐藏。可以设置为 -1 以禁用限制。
    see:
      - minFadeDistance
      - minDistance
      - maxFadeDistance
      - maxDistance
      - minDistanceThreshold
  unk_ds3_f2_20:
    field: int
  unk_ds3_f2_21:
    field: int
  unk_ds3_f2_22:
    field: int
  unk_ds3_f2_23:
    field: int
  unk_ds3_f2_24:
    field: int
  unkDepthBlend1:
    field: float
    default: 1
  unkDepthBlend2:
    field: float
    scale: distance
  premultiplyAlpha:
    field: bool
    default: false
    desc:
      en-US: |
        If enabled, the texture's color will be multiplied by its alpha value, making more transparent parts of the texture darker.
      zh-CN: |
        如果启用，纹理的颜色将乘以其 Alpha 值，使纹理更透明的部分变暗。
  unk_ds3_f2_28:
    field: int
  unk_ds3_f2_29:
    field: float
    scale: distance
    desc:
      en-US: |
        Unknown float.

        This seems to be some sort of distance threshold. When the camera is within this distance, some things will look different in some ways. For example, when within the distance and the {@link blendMode blend mode} is set to {@link BlendMode.Subtract}, the opacity doesn't work the way it normally does. Other effects have been found as well, but what exactly this threshold is for is unknown.
      zh-CN: |
        未知浮点数。

        这似乎是某种距离阈值。当摄像机在此距离内时，某些东西看起来会有所不同。例如，当在距离内且 {@link blendMode 混合模式} 设置为 {@link BlendMode.Subtract 减去} 时，不透明度不会按正常方式工作。还发现了其他影响，但此阈值的具体作用未知。
  shadowDarkness:
    field: float
    desc:
      en-US: |
        Controls how dark shaded parts of the trail are.
      zh-CN: |
        控制拖尾阴影部分的暗度。
  unk_sdt_f2_31:
    field: int
  unk_sdt_f2_32:
    field: bool
    desc:
      en-US: |
        Unknown boolean.

        When enabled, it seems to stop {@link unk_ds3_f2_29} from doing whatever it is doing, and it can also cause some ugly "outline" effects on things seen through particles, but it also fixes an issue where some other particles can be seen through this particle.
      zh-CN: |
        未知布尔值。

        当启用时，它似乎会停止 {@link unk_ds3_f2_29} 的作用，并且可能会对透过粒子看到的内容产生一些难看的“轮廓”效果，但它也修复了某些其他粒子可以透过此粒子看到的问题。
  specular:
    field: int
    resource: texture
    textureType: 3m
    desc:
      en-US: |
        Specular texture ID.
      zh-CN: |
        高光纹理 ID。
    see:
      - lighting
      - glossiness
      - specularity
  glossiness:
    field: float
    default: 0.25
    desc:
      en-US: |
        Controls how sharp the specular highlights are.
      zh-CN: |
        控制高光亮点的清晰度。
    see:
      - lighting
      - specular
      - specularity
  lighting:
    field: int
    default: -1
    desc:
      en-US: |
        Controls how the trail is lit. See {@link LightingMode} for more information.
      zh-CN: |
        控制拖尾的光照方式。有关更多信息，请参阅 {@link LightingMode}。
  unk_sdt_f2_36:
    field: int
    default: -2
  unk_sdt_f2_37:
    field: int
  specularity:
    field: float
    default: 0.5
    desc:
      en-US: |
        Controls how bright the specular highlights are.
      zh-CN: |
        控制高光亮点的亮度。
    see:
      - lighting
      - specular
      - glossiness
  unk_er_f2_39:
    field: int
games:
  DS3:
    fields1:
      - orientation
      - texture
      - normalMap
      - blendMode
      - segmentInterval
      - segmentDuration
      - concurrentSegments
      - segmentSubdivision
      - unk_ds3_f1_8
      - fadeOutTime
      - columns
      - totalFrames
      - attachedUV
      - unk_ds3_f1_13
      - unk_ds3_f1_14
      - unk_ds3_f1_15
    fields2:
      - unk_ds3_f2_0
      - unk_ds3_f2_1
      - unk_ds3_f2_2
      - unk_ds3_f2_3
      - bloom
      - bloomColor
      #
      #
      #
      - unk_ds3_f2_9
      - unk_ds3_f2_10
      - unk_ds3_f2_11
      - unk_ds3_f2_12
      - unk_ds3_f2_13
      - minFadeDistance
      - minDistance
      - maxFadeDistance
      - maxDistance
      - minDistanceThreshold
      - maxDistanceThreshold
      - unk_ds3_f2_20
      - unk_ds3_f2_21
      - unk_ds3_f2_22
      - unk_ds3_f2_23
      - unk_ds3_f2_24
      - unkDepthBlend1
      - unkDepthBlend2
      - premultiplyAlpha
      - unk_ds3_f2_28
      - unk_ds3_f2_29
    properties1:
      - width
      - widthMultiplier
      - startFadeEndpoint
      - endFadeEndpoint
      - color1
      - color2
      - color3
      - alphaFadeThreshold
      - frameIndex
      - frameIndexOffset
      - textureFraction
      - speedU
      - varianceV
      - unk_ds3_p1_13
    properties2:
      - rgbMultiplier
      - alphaMultiplier
      - distortionIntensity
      - unk_ds3_p2_3
      - unk_ds3_p2_4
      - unk_ds3_p2_5
      - alphaThreshold
  SDT:
    fields1:
      - orientation
      - normalMap
      - segmentInterval
      - segmentDuration
      - concurrentSegments
      - segmentSubdivision
      - unk_ds3_f1_8
      - fadeOutTime
      - columns
      - totalFrames
      - attachedUV
      - unk_ds3_f1_13
      - unk_ds3_f1_14
      - unk_ds3_f1_15
    fields2:
      - unk_ds3_f2_0
      - unk_ds3_f2_1
      - unk_ds3_f2_2
      - unk_ds3_f2_3
      - bloom
      - bloomColor
      #
      #
      #
      - unk_ds3_f2_9
      - unk_ds3_f2_10
      - unk_ds3_f2_11
      - unk_ds3_f2_12
      - unk_ds3_f2_13
      - minFadeDistance
      - minDistance
      - maxFadeDistance
      - maxDistance
      - minDistanceThreshold
      - maxDistanceThreshold
      - unk_ds3_f2_20
      - unk_ds3_f2_21
      - unk_ds3_f2_22
      - unk_ds3_f2_23
      - unk_ds3_f2_24
      - unkDepthBlend1
      - unkDepthBlend2
      - premultiplyAlpha
      - unk_ds3_f2_28
      - unk_ds3_f2_29
      - shadowDarkness
      - unk_sdt_f2_31
      - unk_sdt_f2_32
      - specular
      - glossiness
      - lighting
      - unk_sdt_f2_36
      - unk_sdt_f2_37
      - specularity
    properties1:
      - texture
      - blendMode
      - width
      - widthMultiplier
      - startFadeEndpoint
      - endFadeEndpoint
      - color1
      - color2
      - color3
      - alphaFadeThreshold
      - frameIndex
      - frameIndexOffset
      - textureFraction
      - speedU
      - varianceV
      - unk_ds3_p1_13
    properties2: DS3
  ER:
    fields1:
      - orientation
      - normalMap
      - segmentInterval
      - segmentDuration
      - concurrentSegments
      - segmentSubdivision
      - unk_ds3_f1_8
      - fadeOutTime
      - columns
      - totalFrames
      - attachedUV
      - unk_ds3_f1_13
      - unk_ds3_f1_14
      - unk_ds3_f1_15
      - unk_er_f1_14
      - unk_er_f1_15
      - unk_er_f1_16
    fields2:
      - unk_ds3_f2_0
      - unk_ds3_f2_1
      - unk_ds3_f2_2
      - unk_ds3_f2_3
      - bloom
      - bloomColor
      #
      #
      #
      - unk_ds3_f2_9
      - unk_ds3_f2_10
      - unk_ds3_f2_11
      - unk_ds3_f2_12
      - unk_ds3_f2_13
      - minFadeDistance
      - minDistance
      - maxFadeDistance
      - maxDistance
      - minDistanceThreshold
      - maxDistanceThreshold
      - unk_ds3_f2_20
      - unk_ds3_f2_21
      - unk_ds3_f2_22
      - unk_ds3_f2_23
      - unk_ds3_f2_24
      - unkDepthBlend1
      - unkDepthBlend2
      - premultiplyAlpha
      - unk_ds3_f2_28
      - unk_ds3_f2_29
      - shadowDarkness
      - unk_sdt_f2_31
      - unk_sdt_f2_32
      - specular
      - glossiness
      - lighting
      - unk_sdt_f2_36
      - unk_sdt_f2_37
      - specularity
      - unk_er_f2_39
    properties1: SDT
    properties2: DS3
  AC6: ER
  NR: ER
